import styled from "styled-components";

const GeneralDiv = styled.div``;

const General = () => {
    return (
        <GeneralDiv>
            
        </GeneralDiv>
    );
};

export default General;
